# توثيق مسارات التكاملات وجهات الاتصال (Integrations & Contacts API)

## 1. جهات اتصال العميل (Client Contacts)
هذه المسارات تستخدم لإدارة الأشخاص والأرقام التابعة لعميل/شركة واحدة داخل الـ CRM.

**Base URL:** `/api/v1/clients/{client_id}/contacts`

### جلب جهات الاتصال (GET `/`)
- **الوصف:** يجلب جميع جهات الاتصال التابعة لشركة/عميل معين.
- **المخرجات:**
```json
[
  {
    "id": 1,
    "client_id": 40,
    "name": "أحمد",
    "phone": "09999999",
    "email": "ahmed@example.com",
    "position": "مدير المبيعات",
    "is_primary": true
  }
]
```

### إضافة جهة اتصال (POST `/`)
- **المدخلات (Body):**
```json
{
  "name": "سارة",
  "phone": "09888888",
  "email": "sara@example.com",     // اختياري
  "position": "سكرتيرة",             // اختياري
  "is_primary": false              // اختياري (إذا كانت true سيتم إلغاء الأساسي القديم)
}
```

### تعديل جهة اتصال (PUT `/{contact_id}`)
نفس مدخلات الإضافة، ولكن التحديث جزئي (ما يتم إرساله فقط هو ما يتحدث).

### حذف جهة اتصال (DELETE `/{contact_id}`)
يحذف جهة الاتصال من قاعدة البيانات.

### دمج جهة اتصال (POST `/api/v1/clients/{source_client_id}/merge-contact`)
- **الوصف:** لنقل جهة اتصال (رقم جديد) من عميل وهمي إلى عميل حقيقي.
- **المدخلات:**
```json
{
  "target_client_id": 40,   // العميل الحقيقي الذي نريد نقل الرقم إليه (شركة الرشيد)
  "contact_id": 85          // رقم جهة الاتصال التي ننقلها
}
```

---

## 2. تكاملات المزود (Provider Webhooks)
هذه المسارات يستخدمها **مشروع المزود حصراً** للتحدث مع الـ CRM واستكشاف الأرقام.

**Base URL:** `/api/v1/integrations/provider`

### فحص الرقم (POST `/check-contact`)
- **الهدف:** المزود يسأل الـ CRM "لمن يعود هذا الرقم؟".
- **المدخلات:**
```json
{
  "phone": "09999999"
}
```
- **المخرجات (إذا كان موجوداً):**
```json
{
  "exists": true,
  "client_id": 40,
  "contact_id": 15,
  "name": "أحمد"
}
```
- **المخرجات (إذا لم يكن موجوداً):**
```json
{
  "exists": false
}
```

### إنشاء عميل جديد آلياً (POST `/create-lead`)
- **الهدف:** إذا كان الفحص `false`، يطلب المزود من الـ CRM إنشاء ملف جديد لهذا الرقم.
- **المدخلات:**
```json
{
  "phone": "09888888",
  "name": "محادثة واتساب جديدة"
}
```
- **المخرجات:**
```json
{
  "success": true,
  "client_id": 85,
  "contact_id": 90
}
```
