# توثيق الـ API الشامل: التذاكر والتقييمات

هذا التوثيق شامل ومفصل لجميع مسارات الـ API (Endpoints) مع تحديد المدخلات (Inputs) والمخرجات (Outputs) المتوقعة لكل مسار.

**Base URL:** `https://your-domain.com/api`
**Headers Required:**
- `Authorization: Bearer {token}` (لجميع المسارات ما عدا Public)
- `Accept: application/json`

---

## 1. إعدادات التذاكر والتقييمات (الإدارة)

### أ. إدارة تصنيفات التذاكر (Ticket Categories)
هنا يقوم مدير النظام بتعريف أقسام الدعم الفني، ووضع **وقت الـ SLA** الخاص بكل قسم.
*(الـ SLA ليس متغيراً يرسله التطبيق عند إنشاء التذكرة، بل هو إعداد يُضبط هنا، والباك إند يقوم بحسابه تلقائياً لاحقاً).*

**1. جلب التصنيفات (GET `/v1/tickets/categories`)**
- **المخرجات (Response):** مصفوفة بكل التصنيفات شاملة الحقول `(id, parent_id, name, color, icon, sla_hours, is_active)`.

**2. إضافة تصنيف جديد (POST `/v1/tickets/categories`)**
- **المدخلات (Body):**
  ```json
  {
    "name": "أعطال السيرفر",           // إجباري: اسم التصنيف
    "parent_id": null,                 // اختياري: رقم التصنيف الأب (إذا كان هذا تصنيفاً فرعياً)
    "color": "#FF0000",                // اختياري: لون التصنيف
    "icon": "server",                  // اختياري: أيقونة التصنيف
    "sla_hours": 4,                    // اختياري: الوقت المسموح لحل المشكلة بالساعات
    "is_active": true                  // اختياري: التفعيل (افتراضي: true)
  }
  ```

---

### ب. إدارة أنواع التقييمات (Evaluation Types)
لتعريف أنواع التقييمات (مثل: جودة الخدمة، سرعة الرد) لتصنيف تقييمات العملاء.

**1. جلب الأنواع (GET `/v1/evaluations/types`)**
- **المخرجات:** مصفوفة بأنواع التقييمات `(id, name, is_active)`.

**2. إضافة نوع جديد (POST `/v1/evaluations/types`)**
- **المدخلات (Body):**
  ```json
  {
    "name": "أداء فريق المبيعات",
    "is_active": true
  }
  ```

---

## 2. موديول التذاكر (Tickets)

### 1. جلب قائمة التذاكر (GET `/v1/tickets`)
- **الفلاتر المتاحة (Query Params):**
  - `status`: الفلترة حسب الحالة (`open`, `in_progress`, `pending_client`, `resolved`, `closed`).
  - `client_id`: جلب تذاكر عميل محدد.
  - `assigned_to`: جلب التذاكر المسندة لموظف معين.
  - `category_id`: الفلترة حسب التصنيف.
  - `per_page`: عدد النتائج في الصفحة (افتراضي: 15).
- **المخرجات (Response):**
  - بيانات التذكرة كاملة، مع بيانات مبسطة عن (العميل، الموظف المسؤول، ومنشئ التذكرة، والتصنيف).

### 2. إنشاء تذكرة جديدة (POST `/v1/tickets`)
*(ملاحظة: التطبيق لا يرسل وقت الـ SLA. التطبيق يرسل `category_id` فقط، والباك إند سيقرأ الـ `sla_hours` من هذا التصنيف ويحسب موعد الانتهاء تلقائياً).*

- **المدخلات (Body):**
  ```json
  {
    "title": "لا أستطيع الدخول لحسابي",      // إجباري
    "description": "يظهر لي خطأ 500",      // اختياري
    "client_id": 1,                        // اختياري: العميل صاحب المشكلة
    "assigned_to": 2,                      // اختياري: الموظف المسؤول
    "category_id": 1,                      // اختياري: التصنيف الرئيسي
    "sub_category_id": 3,                  // اختياري: التصنيف الفرعي
    "priority": "high",                    // اختياري: (low, medium, high, critical)
    "source": "whatsapp"                   // اختياري: مصدر التذكرة (manual, whatsapp, email, chat_widget, api, phone)
  }
  ```
- **المخرجات (Response):**
  - يعيد بيانات التذكرة مع الرقم المرجعي المولد تلقائياً (مثال: `ticket_number: "#10001"`) ووقت الاستحقاق المحسوب `sla_due_at`.

### 3. تعديل تذكرة (PUT `/v1/tickets/{ticket_id}`)
- **المدخلات المسموحة:** `status`, `client_id`, `assigned_to`, `category_id`, `sub_category_id`, `priority`, `source`, `title`, `description`.
- **ملاحظة:** إذا تم إرسال `status: "resolved"` أو `"closed"`، سيقوم النظام تلقائياً بتسجيل وقت الإغلاق.

### 4. الردود والمراسلات (POST `/v1/tickets/{ticket_id}/messages`)
هذا المسار لإضافة رد من الموظف داخل التذكرة.
- **المدخلات (Body):**
  ```json
  {
    "content": "تم فحص المشكلة وجاري العمل عليها",  // إجباري: نص الرسالة
    "is_internal": false                            // اختياري: هل هي ملاحظة سرية للموظفين فقط أم رد للعميل؟
  }
  ```
- **المخرجات والأثر:**
  - يتم تسجيل الرد باسم الموظف الحالي (من التوكن).
  - إذا كان الرد للعميل (`is_internal: false`)، تتغير حالة التذكرة تلقائياً إلى `pending_client`.

---

## 3. موديول التقييمات (Evaluations)

### 1. جلب التقييمات (GET `/v1/evaluations`)
- **الفلاتر (Query Params):** `client_id`, `assigned_user_id`, `type_id`, `rating`.
- **المخرجات:** بيانات التقييم مع معلومات العميل، الموظف المقيَّم، ونوع التقييم.

### 2. إحصائيات التقييمات (GET `/v1/evaluations/stats`)
- **الفلاتر:** `assigned_user_id`.
- **المخرجات:**
  ```json
  {
    "average": 4.5,
    "total": 120,
    "distribution": {
      "1": 5,
      "2": 2,
      "3": 10,
      "4": 33,
      "5": 70
    }
  }
  ```

### 3. إضافة تقييم مباشرة عبر النظام (POST `/v1/evaluations`)
- **المدخلات (Body):**
  ```json
  {
    "client_id": 1,
    "assigned_user_id": 2,
    "type_id": 1,
    "rating": 5,
    "notes": "الموظف كان متعاوناً جداً",
    "channel": "manual"
  }
  ```
- **الأثر:** يُسجل التقييم، ويُنسخ حقل `notes` تلقائياً كتعليق دائم في الـ Timeline الخاص ببروفايل العميل.

### 4. إنشاء رابط تقييم خارجي للعميل (POST `/v1/evaluations/links`)
- **المدخلات (Body):**
  ```json
  {
    "client_id": 1,
    "assigned_user_id": 2,
    "ticket_id": 104,               // اختياري: إذا كان التقييم مرتبطاً بتذكرة معينة
    "type_id": 1,                   // إجباري: نوع التقييم
    "channel": "whatsapp",
    "note_for_client": "يرجى تقييم الخدمة التي قدمها الموظف"
  }
  ```
- **المخرجات:** يعيد النظام رابطاً عاماً محمياً برمز مشفر (Token) يتم إرساله للعميل.

### 5. إرسال التقييم من قِبل العميل (الواجهة العامة)
**هذا المسار لا يحتاج مصادقة (No Auth)**. يُستخدم في صفحة التقييم التي يفتحها العميل من الرابط.
- **الرابط:** `POST /public/evaluations/{token}`
- **المدخلات (Body):**
  ```json
  {
    "rating": 4,
    "notes": "الخدمة سريعة وممتازة"
  }
  ```
- **الأثر:**
  - يتم تسجيل التقييم في النظام وحساب الإحصائيات.
  - يتم إتلاف الرابط (Token) لكي لا يُستخدم مرة أخرى.
